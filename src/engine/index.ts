/**
 * Data Engine — foundation only (see docs/DATA_ENGINE.md).
 *
 * This module is not yet wired into any React page. Nothing under
 * src/pages, src/components, or src/store imports from here — that's
 * deliberate future work once a real importer exists.
 */
export * from './types'
export * from './compatibility'
export * from './displayNames'
export * from './importer'
